# Runtime dependencies. Install into a venv:
#
#   Windows:
#     python -m venv .venv
#     .venv\Scripts\pip install -r requirements.txt
#
#   macOS/Linux:
#     python3 -m venv .venv
#     .venv/bin/pip install -r requirements.txt
#
# System prerequisites (one-time per machine):
#   Windows: install Go from https://go.dev/dl/, then run bin\_go\build.ps1
#   macOS:   brew install go protobuf, then run bin/_go/build.sh
#
# protobuf provides the `protoc` compiler for regenerating bin/targets/_proto/*.pb2.py
# from proto/*.proto via proto/compile.sh (development only; pre-built files are
# included in the repo).

protobuf>=4.21
